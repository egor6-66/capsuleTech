---
tags: [english, lessons, for-teacher, import]
status: action-needed — две правки в vault
author: architect
re: lessons-model-final.md
---

# Импорт vault, раунд 1: конвейер работает, нужны две правки контента

> [!info] Importer построен и прогнан по живому `D:\learn\lang`:
> **imported=0, rejected=15** — и это ПРАВИЛЬНО: все отказы контентные,
> с понятными причинами, ровно по правилам валидации из [[lessons-model-final]].
> Никаких обходов мы не делали (правило «слово сначала в словарь» соблюдено).

## Что нужно от тебя (две вещи)

### 1. `type:` во frontmatter справочников — 14 файлов
Все `grammar/`, `phonetics/`, `speech/` отклонены с «нет type». По финалочке:
дописать в frontmatter каждого:

```yaml
type: rule
```

Тело не трогаешь — парсится как есть. (Это то самое «добавляем только type»
из финалочки, §Rule.)

### 2. Слово `already` — завести в словарь
Эталонный дрилл [[past-perfect-which-clause]] ссылается на
`words: [eat, call, leave, come, already]` — всё есть, кроме **already**.
Пришли запись для словаря (как обычно для teacher-vocab): оригинал, перевод,
фонетика (pron_ru), уровень, часть речи — заведём штатным словарным флоу,
и дрилл пройдёт.

## Каскад, чтобы было понятно
Дрилл отклонён по двум независимым причинам: (а) его `rule:
grammar-verbs-tenses` не импортировался (см. п.1) → ссылка не резолвится;
(б) `already` нет в словаре. После твоих правок — переимпорт, и раунд 2
должен дать rejected=0 по эталону.

## Дальше
Раунд 2 зелёный → уроки появляются в базе → следующим шагом апп начинает
их показывать (read-only путь: концепт → правило → дрилл текстом).
