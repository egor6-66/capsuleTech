---
title: backend/lang — tense-валидатор: вернуть семантику финалочки (context засчитывается) + частотные маркеры
status: ready (блокирует пачку 2: 3 tense-дрилла reject)
audience: owner-сессия `claude-scope -Scope backend-lang` (commit-only, без push)
last_updated: 2026-07-05
adr_refs: [069]
---

# Контекст (round-4 отчёт architect'а)

Пачка 2: 3 tense-дрилла reject'нулись, хотя контент валиден по финалочке.
Корень — валидатор строже финалочки: требует НАЙТИ словарный маркер в
promptRu∪context, а финалочка (`lessons-model-final.md`, Item.context):
«**Обязателен, если в promptRu нет маркера времени**» — т.е. САМО НАЛИЧИЕ
context снимает неоднозначность (context — человеческий текст, машиносмысл
не проверяем). Плюс словарь маркеров без частотных («каждый день») и «когда».

# Scope (backend/lang, lessons_importer)

1. **Семантика tense-проверки = финалочка:** item валиден, если
   (а) словарный маркер найден в `promptRu`, ИЛИ (б) `context` непустой.
   Поиск маркера в тексте context — УБРАТЬ (context засчитывается фактом).
2. **Словарь `_TIME_MARKERS` дополнить** (для case «без context»):
   «когда» (клаузы «когда X» — главный маркер two-action дриллов),
   частотные «каждый/каждую/каждое», «обычно», «всегда», «часто», «редко»,
   «иногда», «никогда», «только что», «уже когда-нибудь/когда-либо»,
   времена суток «утром/днём/вечером/ночью», «в детстве», «на прошлой неделе»,
   «в прошлом году», «вдруг». Коммент «расширяемый» остаётся.
3. Тесты: item с context без словарного маркера — ПРОХОДИТ (tense);
   item без context с «каждый день»/«когда … позвонил» — проходит;
   item без context и без маркера — reject (не ослабить совсем!).
4. Перегнать `vault_import D:\learn\lang` → ожидание: пачка 2 полностью
   (4 дрилла + words/batch2 8 слов), rejected=0. Отчёт в коммит.

# Что НЕ делаем
`dimension`-механику не трогаем (работает); vault-контент не правим (валиден).
