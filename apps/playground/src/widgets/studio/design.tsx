/**
 * Studio.Design — Design-workspace панель (рендерится в <Outlet/> каркаса).
 * Пока = контекстный инспектор; линзы ui/style/text/data наполнят его позже.
 */
const Design = Widget(() => <Views.Studio.Inspector />);

export default Design;
