/**
 * /workspace/home — приватная главная (landing после входа).
 *
 * Тонкий Page: контент (приветствие) живёт в `Widgets.Home`
 * (Ui.Button доступен только на Widget-уровне).
 */
const Home = Page(() => <Widgets.Home />);

export default Home;
