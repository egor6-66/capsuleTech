const Inspector = Widget((Ui) => (
  <Ui.Layout.Flex direction={'col'} h={'full'} w={'full'} fluid={300}>
    <WebStudio.Styles />
    <WebStudio.Props />
  </Ui.Layout.Flex>
));

export default Inspector;
