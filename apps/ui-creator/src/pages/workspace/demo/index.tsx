/**
 * Демо (`/workspace/demo`) — площадка для проверки собранного UI.
 * Пока плейсхолдер.
 */
const Demo = Page(() => (
  <div class="flex h-full items-center justify-center text-foreground/50">Демо</div>
));

export default Demo;
