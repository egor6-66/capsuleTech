//! Доменные модули. Каждый — самодостаточный кусок (роуты + логика + запросы
//! к БД), который позже выносится в отдельный микросервис.
//!
//! Сейчас только `health`. Дальше добавятся: `serve` (раздача билдов),
//! `builds`, `users`, `auth`.

pub mod health;
