-- 0001_init — первая миграция (каркас).
--
-- Реальные таблицы (users, builds, auth_identities) приедут отдельными
-- миграциями на следующих шагах. Этот файл фиксирует, что migration-движок
-- подключён и прогоняется на старте сервера.
SELECT 1;
