# @capsuletech/shared-utils

> Placeholder. Не публикуется (`private: true`), `src/index.ts` пустой
> (`export {}`). Раньше тут жил `z`-расширение, переехало в
> [`@capsuletech/shared-zod`](../zod). Реальных потребителей сейчас нет.

Пакет оставлен на случай появления настоящего общего cross-package утилити-слоя. Если такого долго не появится — удалить вместе с manifest-references (`nx.json` его не упоминает, потому что не релизный; `tsconfig.base.json` тоже).
