import { Outlet, createRootRoute } from '@tanstack/solid-router';

// Если нужен typed router context (для guards / isAuthenticated / ...) —
// поменяй на `createRootRouteWithContext<MyContext>()` и пробрось значение
// через <BaseProviders routerContext={...}> в .capsule/bootstrap.tsx.
// См. docs/_meta/web-router.md (грабли #6-7) и ADR 014.

export const Route = createRootRoute({
  component: () => (
    <>
      <Outlet />
    </>
  ),
});
