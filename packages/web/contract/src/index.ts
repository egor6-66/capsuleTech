/**
 * @capsuletech/web-contract — SKELETON (0.0.0).
 *
 * Leaf-протокол контрактов компонентов (zero-dep). Компонент КАРМАНИТ свой
 * контракт через `defineContract([...rules])`; редакторы/тесты/доки ПОТРЕБЛЯЮТ
 * через `collectContracts()`. Контракт = композиция rule-примитивов (плагинов).
 *
 * Стюард — главный (контракт/архитектура, критический путь). План:
 *   docs/playground/contracts.md + docs/playground/roadmap.md (F0).
 *
 * Старт: ОДИН rule (`accepts`/`isLeaf`) на Button → эталон → плагины.
 */
export {};
