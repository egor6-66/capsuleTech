/**
 * @capsuletech/web-auth/ui — config-driven form-блоки на @capsuletech/web-ui.
 *
 * Правило: ТОЛЬКО @capsuletech/web-ui примитивы — никакого raw div/native.
 * Headless-апп НЕ подключает этот subpath.
 */

export { AuthGateSwitch } from './gateSwitch';
export { AuthLoginForm } from './loginForm';
