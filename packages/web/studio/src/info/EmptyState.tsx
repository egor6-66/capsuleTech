/**
 * EmptyState — заглушка info-панели, когда пресет не выбран.
 */

export const EmptyState = () => (
  <div class="px-3 py-4 text-xs text-muted-foreground">
    Выберите пресет в палитре, чтобы увидеть контракт и описание.
  </div>
);
