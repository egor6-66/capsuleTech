// @capsuletech/web-menu — main barrel.
//
// Data-driven menu (Menus.*, ADR 044). web-ui = Kobalte owner; web-menu is the
// zero-Kobalte data-driven layer. Currently SKELETON (0.0.0) — contract only.

export * from './core';
