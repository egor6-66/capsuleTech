/**
 * shared/dragChip — презентационный атом drag-overlay студии (чип с иконкой +
 * названием таскаемого узла). Zero internal deps: web-dnd + web-ui/manifest +
 * solid. Единственный потребитель — `core/provider` (`<DragOverlay>`).
 */

export { DragChip } from './DragChip';
